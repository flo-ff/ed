services:
  preview:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080" # sketchy — origin/main
      - "8081:8081" # clean  — explore/clean-modern
    restart: unless-stopped
