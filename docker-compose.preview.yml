services:
  preview:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8082:8082" # sketchy — origin/main
      - "8083:8083" # clean  — explore/clean-modern
    restart: unless-stopped
